angular.module('pot.directives', [])
	/*.directive('potSlot', function() {
		return {
			restrict: 'A',
			template: '<div class="pot-slot">' +
			          '	<img data-ng-src="img'
		}
	});
	.directive('foodButton', function() {
		return {
			restrict: 'E',
			template: '<a class="btn" data-ng-click="addToPot(f.id)"><img data-nd-src="{{ f.img }}<span>{{ f.name }}</span></a>',
			replace: true
		}
	})

*/